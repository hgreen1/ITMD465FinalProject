-------ReadMe-------

!!! NOTE !!!
If using Chrome/Chromium you have to use the --disable-web-security flag to open the html file, so the HTML can access the CSV file.

It's a basic data visualization using d3.js. The file was imported from the web so an internet connection is *required*.

There are two pages. the index page is a basic pie chart showing the frequency of all the crimes from 2001 to 2016 separated by year.
The page2.html houses the 